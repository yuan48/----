        alt+shift+↓,可以复制上一行
        br*2可以直接加入<br><br>
        快速生成一段文本：<p>lorem</p>

        ctrl+/快速注释

        <P>块元素，不用换行
        <span>行元素 用换行

        ul>li，会直接生成包含li标签的ul结构
        ul>li*5，会直接生成包含5个li标签的ul结构

        tab+↓可以直接在下一个标签中间打字

        .box1+enter可以直接生成标签