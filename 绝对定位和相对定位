*相对定位
position:relative
偏移量:top left right bottom
开启后相对于元素原来在文档流中的位置移动


*绝对定位
position:absolute
脱离文档流后，行内会变成块元素
并且脱离文档流后，块元素的宽度和高度默认都被内容撑开

此时使用绝对定位的元素层级会向上移动一层，会盖住下面的元素

relative的0,0坐标是自己的父元素
absolute的0,0坐标是离它最近的开启了定位的祖先元素(包含块)

html是初始包含块